# はじめにお読みください（クイックスタートガイド）

このフォルダを受け取った方へ：  
本パッケージは、お手持ちの **Mac mini** を「プライベートなAIサーバー」にして、手元の **MacBook・iPhone・iPad** などからChatGPTのような快適な画面でAIを使えるようにするためのセットアップツールです。

---

## 🎯 このパッケージでできること

1. **完全プライベートなAIチャット**  
   質問やアップロードした文書が外部の企業やクラウドに送信されず、すべてお手元の Mac mini の中だけで処理されます。
2. **ChatGPTと同じ使い心地（Open WebUI）**  
   使いやすいWeb画面で、文字入力やファイル添付、過去の履歴管理がかんたんに行えます。
3. **どこからでも安全にアクセス（Tailscale）**  
   インターネット全体に公開するのではなく、あなた自身の専用ネットワーク（Tailscale）経由でのみ繋がるため、安全です。
4. **既存の環境を壊さない安全設計**  
   すでに動いている他のアプリやネットワーク設定を勝手に消したり書き換えたりしません。

---

## 📋 事前に用意するもの

セットアップを始める前に、**Mac mini** に以下が用意されているかご確認ください。

| 必要なもの | 説明 | 入手先 |
|---|---|---|
| **Mac mini** | M1 / M2 / M4などのApple Silicon搭載Macを推奨 | 本体 |
| **Ollama** | AIモデルをMac上で動かすための基本アプリ | [公式ページ（ollama.com）](https://ollama.com/) からインストール |
| **Tailscale** | 外出先や手元の端末と安全に繋ぐVPNアプリ | Mac App Store または [公式サイト](https://tailscale.com/) からログイン |
| **uv** | Pythonの高速セットアップツール | ターミナルで `curl -LsSf https://astral.sh/uv/install.sh \| sh` |

---

## 🚀 最短導入手順（3つのステップ）

黒い画面（ターミナル）で難しいコマンドを打つ必要はありません。  
**マウスのダブルクリックと、画面に出てくる番号を選ぶだけ**で完了します。

### ステップ1：Mac mini にフォルダを置く
このフォルダ全体を、サーバーとして動かしたい **Mac mini** のデスクトップなどに置きます。  
※操作はすべて Mac mini 側で行います（MacBook側の設定は不要です）。

### ステップ2：`setup.command` をダブルクリックする
フォルダ内にある **`setup.command`** をダブルクリックして開きます。

> 💡 **もし「開発元が未確認」などの警告で開けない場合**  
> ファイルを右クリック（二本指タップ）して「開く」を選択してください。

黒い画面が表示され、以下のようなメニューが出ます。

```text
0 現状確認（読み取りだけ）
1 専用Python / WebUIの導入
2 Ollamaを起動
3 試験用モデルを取得
4 WebUIを起動（初回はMac mini内だけ）
5 管理者作成後、安全設定を反映
6 Tailscaleへ専用HTTPS経路を追加
7 診断
8 バックアップ
q 終了
```

### ステップ3：番号を順番に入力する

キーボードで半角数字を入れて `Enter` を押していきます。

1. **`0` を入力して `Enter`（現状確認）**  
   パソコンの準備状態をチェックします。何も変更しないので安心してください。
2. **`1` を入力して `Enter`（初期セットアップ）**  
   AIの画面に必要な環境を自動で構築します。
3. **`2` を入力して `Enter`（AIエンジンの起動）**  
   Ollamaを起動します。
4. **`3` を入力して `Enter`（AIモデルのダウンロード）**  
   お試し用のAIモデル（Qwen3など）をダウンロードします（数分かかります）。
5. **`4` を入力して `Enter`（Web画面の起動）**  
   Mac miniの中で画面を立ち上げます。
6. **Mac mini のブラウザで初期登録する**  
   Mac mini のSafariなどで `http://127.0.0.1:3001` を開きます。  
   （3001番が他のツールに使われていた場合は、セットアップ時に自動で近い空き番号が割り当てられ、その番号が画面に表示されます）  
   「最初の管理者」の作成画面が出るので、あなたのお名前・メールアドレス・パスワードを登録してログインします。
7. **メニューに戻り、`5` を入力して `Enter`（安全ロック）**  
   先ほど登録した管理者のメールとパスワードを入力します。  
   外部からの勝手な新規登録を禁止し、セキュリティを強化します。
8. **`6` を入力して `Enter`（接続URLの発行）**  
   手元のMacBookやiPhoneから接続するための **専用URL（https://...）** が画面に表示されます！
9. **`7` を入力して `Enter`（最終チェック）**  
   すべての設定が正しく完了しているかを診断します。

これでセットアップ完了です！  
画面に表示されたURLを手元のMacBookやiPhoneのブラウザで開けば、いつでもAIチャットが使えます（※手元の端末でもTailscaleをONにしておいてください）。

---

## ⚠️ 安全に関する大切なお知らせ

- **初期検証用モデルについて**  
  最初にダウンロードされるAIモデルは動作確認用です。Mac miniのメモリ量に応じた本格的なモデル選定は、動作確認後に行うことをおすすめします。
- **大切な実データの投入について**  
  セットアップ直後は、まずテスト用の会話やダミー文書で使い勝手や動作を試してください。患者情報・顧客情報・極秘ファイルなどの重要な実データを投入するのは、バックアップや運用の確認が取れてからにしてください。
- **Mac miniの電源**  
  Mac miniがスリープすると外部から接続できなくなります。「システム設定」＞「省エネルギー」で、ネットワークアクセス時にスリープ解除されるよう設定しておくと快適です。

---

## 📖 より詳しい説明書

さらに詳しい仕様やカスタマイズ方法、万が一動かないときの対処法については、以下のドキュメントをご覧ください。

- **[構築手順書・詳細ガイド（docs/local-llm-web-mvp_BUILD_GUIDE.md）](docs/local-llm-web-mvp_BUILD_GUIDE.md)**  
  図解付きの分かりやすい解説から、トラブルシューティング、技術的詳細まで網羅した完全ガイドです。
- **[全体README（README.md）](README.md)**  
  開発背景や各コマンドの詳細な仕様一覧です。
