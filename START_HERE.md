# BOSS：最初はここだけ

## いま渡しているもの

前に決めた構成をMac miniへ入れるための**実装コード一式**です。Open WebUIの画面を作り直したものではありません。Mac miniへのインストールは、まだ行っていません。

## 最初の操作

1. このZIPを**Mac mini**で展開する。
2. 中の`setup.command`を開く。
3. **0（現状確認）**を選ぶ。

0は読むだけなので、OpenClawやネットワークの設定は変えません。

ターミナルからなら、展開したフォルダで次を実行します。

```bash
bash ./llmweb preflight
```

ここでOllama/uvが見つからない、11434番が使用中などと出たら、その準備を先に行います。**既存OpenClawを止めたり、Tailscaleをリセットしたりはしません。**

## その後の順序

メニュー1→2→3→4まで進め、Mac miniのブラウザで管理者を作成。その後5→6→7です。番号を順に押せばすべて自動で確認済みになるわけではなく、エラーが出たらその工程で止めてください。詳しい説明は[README](README.md)にあります。

最初のモデルは動作試験用の候補です。Mac miniのメモリに合わせた調整、MacBookからの実利用、文書添付、再起動後の復帰は導入後に確認します。

このパッケージの自動テストは通っていますが、**Mac実機での動作確認完了ではありません**。大切な資料や患者・顧客情報は、まだ入れないでください。
